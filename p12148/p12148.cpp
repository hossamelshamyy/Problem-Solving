// Not solved

#include <bits/stdc++.h>
#include <math.h>

#include <iostream>
using namespace std;
// function f(x) { return 28 + (x + Math.floor(x/8)) % 2 + 2 % x + 2 * Math.floor(1/x); }

int numOfDays(int x) {
    a1 = 
    int ans = (x + int(floor((x/8)))) % 2 + 2 % x + 2 * floor(1 / x);
    return 28 + ans;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    return 0;
}